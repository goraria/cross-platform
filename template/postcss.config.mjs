const config = {
  // plugins: ["@tailwindcss/postcss"],
  plugins: {
    "@tailwindcss/postcss": {},
    // tailwindcss: { config: './tailwind.config.ts' },
    // autoprefixer: {},
  }
};

export default config;
